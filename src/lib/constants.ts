export const CELL_SIZE = 400;
export const GRID_SIZE = 4;
export const SCREEN_SIZE = CELL_SIZE * GRID_SIZE;
export const GAP = 32;
export const ROUND_CORNER = 32;
export const SPEED = 0.3;
export const TILE_SIZE = (SCREEN_SIZE - (GRID_SIZE + 1) * GAP) / GRID_SIZE;
