export const CELL_SIZE = 100;
export const GRID_SIZE = 4;
export const SCREEN_SIZE = 400;
export const GAP = 8;
export const ROUND_CORNER = 8;
export const TILE_SIZE = (SCREEN_SIZE - (GRID_SIZE + 1) * GAP) / GRID_SIZE;